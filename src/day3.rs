pub fn solve() {
    let (part_one, part_two) = solution1::solve();

    dbg!(part_one);
    dbg!(part_two);

    assert_eq!(part_one, todo!());
    assert_eq!(part_two, todo!());
}

pub mod solution1 {
    pub fn solve() -> (u32, u32) {
        let data = include_str!("../data/day3.txt");
        
        (0,0)
    }
}
